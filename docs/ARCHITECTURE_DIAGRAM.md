# Architecture Diagram

Use `architecture.svg` as the submission architecture diagram. It can be opened directly in a browser and exported to PNG from a browser or image editor if the form requires PNG.
